pandas>=2.2
numpy>=1.26
pyarrow>=16.0
scikit-learn>=1.5
lightgbm>=4.3
shap>=0.46
matplotlib>=3.9
fastapi>=0.111
uvicorn>=0.30
pydantic>=2.7
anthropic>=0.40
python-dotenv>=1.0
joblib>=1.4
httpx>=0.27
duckdb>=1.0

# MCP server. v2 of the SDK targets spec revision 2026-07-28 (stateless core);
# the v1 line does not. Pin the major version — v1 and v2 share the package name.
mcp>=2,<3

# Tests
pytest>=8.0
